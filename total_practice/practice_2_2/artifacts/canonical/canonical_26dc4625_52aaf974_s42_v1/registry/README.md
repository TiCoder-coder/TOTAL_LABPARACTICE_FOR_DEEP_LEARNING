# Phase 5 verified copy

The checkpoint and output trees beside this directory are byte-identical verified copies. They are not active authority in Phase 5.

The inactive draft registry is `configs/canonical_registry_phase5_draft.json`. The active registry continues to reference the original resources.
