# Canonical Source Package

Use `practice_2_2.*` as the recommended/default import. Its resource resolvers
read `configs/canonical_registry.json`, whose authority is the new project
layout. The historical `processing_own_phase.*` package remains available only
for regression compatibility and is not removed in Phase 6.
